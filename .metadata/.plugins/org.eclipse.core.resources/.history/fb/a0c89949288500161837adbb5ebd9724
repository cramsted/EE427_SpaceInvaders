/*
 * controls.c
 *
 *  Created on: Sep 27, 2016
 *      Author: superman
 */
#include "controls.h"
#include <stdio.h>

void readInput(){
	char str[100];
	xil_printf("enter a value: ");
	if (fgets(str, 100, stdin)) {
		return; // no input
	}

	xil_printf("you entered %s\n\r", str);
}
